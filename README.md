# Rubik’s Cube Solver

A fullstack Rubik's cube solving robot that takes cube layouts as inputs and executes them physically to solve the cube. Visit the rubiks cube solver project on my website for more info.

## Stack

- `React` frontend  
- `Node.js` + `Express` backend  
- Uses `cubejs` library for solving  
- ESP32 firmware in `embedded.ino`